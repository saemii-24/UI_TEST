import { render, screen, fireEvent, act, waitFor } from "@testing-library/react";
import { vi } from "vitest";
import ImageCard from "@/components/ImageCard";

describe("ImageCard 테스트", () => {
  it("fetch된 데이터를 가지고 ImageCard가 적절히 렌더링 된다.", () => {});
});
